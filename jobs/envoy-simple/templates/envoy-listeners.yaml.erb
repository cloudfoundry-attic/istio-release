# this file is being watched for MOVES via inotify
# to trigger a dynamic update without restarting envoy,
# write the updates to a new file and then
#    mv envoy-listeners.yaml.new envoy-listeners.yaml
version_info: "0"
resources:
- '@type': type.googleapis.com/envoy.api.v2.Listener
  name: listener_3307
  address:
    socket_address:
      address: 0.0.0.0
      port_value: 3307
  filter_chains:
  - filters:
    - name: envoy.tcp_proxy
      config:
        stat_prefix: ingress_3307
        cluster: local_mysql
