apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: cloudfoundry-ingress
spec:
  servers:
  - port:
      number: 80
      protocol: http
    hosts:
    - "*"
  - port:
      number: 443
      protocol: http
    hosts:
    - "*"
