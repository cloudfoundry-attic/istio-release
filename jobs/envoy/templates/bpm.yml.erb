---
processes:
- name: envoy
  executable: /var/vcap/packages/envoy/bin/envoy
  args:
  - -c /var/vcap/jobs/envoy/config/envoy.json
  - --max-obj-name-len 200
  - --service-cluster x
  - --service-node router~x~x~x
  - --v2-config-only
  capabilities:
  - NET_BIND_SERVICE
  hooks:
    pre_start: /var/vcap/jobs/envoy/bin/bpm-pre-start
